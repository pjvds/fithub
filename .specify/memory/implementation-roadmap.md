# FitHub Implementation Roadmap

**Tech Stack Locked:** TypeScript + SST v4 + Cloudflare (Workers, D1, KV, Queues, R2, Durable Objects) + Astro web frontend  
**Generated:** 2026-05-05  
**Last Amended:** 2026-05-06 (mobile postponed; web frontend (Astro) added as primary client)  
**Status:** Backend implementation in progress (000-backend-foundation); web frontend spec complete

> **Pivot note (2026-05-06):** Native mobile development is **postponed**. The primary v1 client is a web app (Astro on Cloudflare Pages). Specs 002–004 (Zwift/Strava/Apple Health mobile) are **superseded or postponed**. See individual spec files for status banners. When mobile is revived, the framework decision will be re-evaluated from scratch.

---

## Active Feature Roadmap

| Feature | Spec | Status | Tasks | Notes |
|---------|------|--------|-------|-------|
| **Backend Foundation** | `000-backend-foundation` | 🟡 In Progress | 88 tasks | Auth Workers, API Workers, Sync Orchestrators, Dedup engine; Phases 1–5 |
| **Web Frontend** | `005-web-frontend` | 📋 Planned | TBD | Astro on Cloudflare Pages; plan.md + research.md + data-model.md + contracts/ complete |
| **Zwift OAuth (web)** | `006-zwift-oauth-web` | 🔜 Not started | TBD | Web OAuth flow for Zwift; spec not yet created |
| **Strava OAuth (web)** | `007-strava-oauth-web` | 🔜 Not started | TBD | Web OAuth flow for Strava; spec not yet created |

## Superseded / Postponed Features

| Feature | Spec | Status | Notes |
|---------|------|--------|-------|
| Zwift OAuth (mobile) | `002-zwift-oauth` | ⛔ SUPERSEDED | Replaced by `006-zwift-oauth-web` |
| Strava OAuth (mobile) | `003-strava-oauth` | ⛔ SUPERSEDED | Replaced by `007-strava-oauth-web` |
| Apple Health | `004-apple-health-integration` | ⏸ POSTPONED | HealthKit is iOS-only; revisit when native mobile client is on roadmap |


---

## Architecture Overview

```

         Web App (Astro on Cloudflare Pages)              │
         app.fithub.app                                   │

                                                          │
 API Worker (Hono)          api.fithub.app              │
  - GET /api/me, /connections, /activities               │
  - POST /sync/trigger, /errors                          │
  - DELETE /user                                         │
                                                          │
 Auth Worker (OpenAuth.js)  auth.fithub.app             │
  - OAuth PKCE flow (Zwift, Strava)                      │
  - JWT issuance + JWKS endpoint                         │
  - HttpOnly session cookie (apex .fithub.app)           │
                                                          │
 Sync Durable Object        (per-user)                  │
  - Sync cursor, locks, in-flight job state              │
                                                          │
 Outbox Relay Worker        (cron: every minute)        │
  - Drains outbox_events → EventBus queue                │
                                                          │
 D1 Database                (Cloudflare D1 / SQLite)    │
  - users, connections, activities, outbox_events        │
  - canonical_activities, dedup_groups                   │
                                                          │
 KV                         (Cloudflare KV)             │
  - Activity feed cache, JWKS cache                      │
                                                          │
 R2                         (Cloudflare R2)             │
   - Raw platform payloads, FIT/TCX file archive          │
```

---

## Next Steps

1. **Continue `000-backend-foundation` implementation** — Phase 1 (Auth Worker) is the unblocking dependency for everything else
2. **Run `/speckit-tasks` for `005-web-frontend`** — generate tasks.md from spec.md + plan.md now that all planning artifacts are complete
3. **Create `006-zwift-oauth-web` spec** — run `/speckit-specify` once the backend Auth Worker is ready
4. **Create `007-strava-oauth-web` spec** — parallel to 006

---

**Version:** 2.0.0 (post-mobile-pivot)  
**Status:** Active  
**Generated By:** Copilot speckit-plan workflow  
**Last Updated:** 2026-05-06
